# k8 Configs

---

## Overview
Here we store all the .yaml files for the services.

  (**Note**: By keeping all the .yaml files in the "k8s-configs" folder, we can load them all simultaneously, instead of executing them one by one)
