apiVersion: v1
kind: Service
metadata:
  name: manager-service
  namespace: default
spec:
  type: NodePort
  selector:
    app: manager-api
  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: 8000
      nodePort: 30000 # Your powershell script will now send jobs to this port